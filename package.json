{
  "name": "root",
  "workspaces": [
    "./packages/*"
  ],
  "scripts": {
    "install": "npm install && cp -L -r ./node_modules ./packages/web"
  },
  "devDependencies": {
    "lerna": "^6.0.1"
  }
}
